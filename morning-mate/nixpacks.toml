[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["COREPACK_INTEGRITY_KEYS=0 corepack enable && COREPACK_INTEGRITY_KEYS=0 corepack prepare pnpm@9.15.9 --activate && pnpm i --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "node dist/index.js"
